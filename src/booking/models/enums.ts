export enum BookingState {
  PENDING = 'pending',
  APPROVED = 'approved',
  DISAPPROVED = 'disapproved',
  CANCELLED = 'cancelled',
  CONFIRMED = 'confirmed',
  COMPLETED = 'completed',
  MISSED = 'missed',
}
